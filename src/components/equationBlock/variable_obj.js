class VariableObj {
    constructor(label, info, min, max, defaultValue) {
        this.label = label
        this.info = info
        this.min = min 
        this.max = max 
        this.defaultValue = defaultValue
    }
}

export default VariableObj