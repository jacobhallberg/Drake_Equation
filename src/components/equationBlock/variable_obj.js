class VariableObj {
    constructor(label, info, min, max, defaultValue, step) {
        this.label = label;
        this.info = info;
        this.min = min;
        this.max = max;
        this.defaultValue = defaultValue;
        this.step = step;
    }
}

export default VariableObj;
